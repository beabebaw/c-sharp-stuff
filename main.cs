/* this script will call all that previous functions that i made
in this order:

- create user
create username, password and email
keep username and password

- request token
use the username and password
keep the token

- use access token to request job
use the token
keep the job id
keep the job type
keep the string that will be worked on

in no particular order:

- encode string
use the string
send the id job
send the result

- decode base64
use the string
send the id job
send the result

- check gene
use the string
send the id job
send the result */
